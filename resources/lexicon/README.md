# Local-only lexicon payload

The expected payload is `most_frequent_words.csv`, but its source, license,
redistribution terms, and training eligibility are not verified. Keep that
file local and ignored. Do not commit it until its provenance and permitted
uses are documented in `resources/manifest.yaml` and `DATA_SOURCES.md`.
